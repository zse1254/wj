name = "wj"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

pages_build_output_dir = ".vercel/output/static"

# Environment variables are set in Cloudflare Dashboard (not here) to avoid binding conflicts
